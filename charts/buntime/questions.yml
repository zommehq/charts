# AUTO-GENERATED FILE - DO NOT EDIT
# Generated by: bun scripts/generate-helm-questions.ts
# Source: charts/questions.base.yaml + plugins/*/manifest.yaml
#
# To regenerate: bun scripts/generate-helm-questions.ts

questions:
  - variable: buntime.logLevel
    label: Log Level
    description: Application log verbosity
    type: enum
    default: info
    options:
      - debug
      - info
      - warn
      - error
    group: Runtime
  - variable: buntime.poolSize
    label: Worker Pool Size
    description: Maximum number of concurrent workers
    type: int
    default: 100
    min: 1
    max: 1000
    group: Runtime
  - variable: replicaCount
    label: Replica Count
    description: Number of Buntime pod replicas
    type: int
    default: 1
    min: 1
    max: 10
    group: Runtime
  - variable: service.type
    label: Service Type
    description: Kubernetes service type
    type: enum
    default: NodePort
    options:
      - ClusterIP
      - NodePort
      - LoadBalancer
    group: Runtime
  - variable: buntime.apiPrefix
    label: API Prefix
    description: Prefix for runtime API routes (e.g., '/_' mounts API at '/_/api')
    type: string
    default: ""
    group: Runtime
  - variable: buntime.pluginDirs
    label: Plugin Directories
    description: Colon-separated list of plugin directories
    type: string
    default: /data/.plugins:/data/plugins
    group: Runtime
  - variable: buntime.workerDirs
    label: Worker Directories
    description: Colon-separated list of worker directories
    type: string
    default: /data/.apps:/data/apps
    group: Runtime
  - variable: image.tag
    label: Image Tag
    description: Docker image tag (use 'latest' for most recent)
    type: string
    default: latest
    group: Runtime
  - variable: ingress.host
    label: Ingress Hostname
    description: Hostname for the ingress (e.g., buntime.example.com). Leave empty to disable ingress.
    type: hostname
    default: ""
    group: Runtime
  - variable: ingress.className
    label: Ingress Class
    description: Ingress controller class name
    type: string
    default: traefik
    group: Runtime
  - variable: ingress.tls.enabled
    label: Enable TLS
    description: Enable HTTPS with TLS certificate
    type: boolean
    default: false
    group: Runtime
  - variable: persistence.plugins.size
    label: Plugins Volume Size
    description: Size of persistent volume for custom plugins
    type: string
    default: 5Gi
    group: Runtime
  - variable: persistence.plugins.storageClass
    label: Plugins Storage Class
    description: Storage class for plugins volume (leave empty for default)
    type: storageclass
    default: ""
    group: Runtime
  - variable: persistence.apps.size
    label: Apps Volume Size
    description: Size of persistent volume for user applications
    type: string
    default: 10Gi
    group: Runtime
  - variable: persistence.apps.storageClass
    label: Apps Storage Class
    description: Storage class for apps volume (leave empty for default)
    type: storageclass
    default: ""
    group: Runtime
  - variable: plugins.database.libsqlUrl
    label: LibSQL Primary URL
    type: string
    group: Database
    description: URL of the primary LibSQL server (e.g., http://libsql:8080)
    default: http://libsql:8080
    required: true
  - variable: plugins.database.libsqlReplicas
    label: LibSQL Replica URLs
    type: multiline
    group: Database
    description: Optional list of replica URLs for read scaling. One URL per line.
    default: ""
  - variable: plugins.database.libsqlAuthToken
    label: LibSQL Auth Token
    type: password
    group: Database
    description: Authentication token for LibSQL (leave empty if not required)
  - variable: plugins.deployments.excludes
    label: Exclude Patterns
    type: string
    group: Deployments
    description: Folder patterns to exclude, comma-separated (e.g., .cache, cli, runtime)
    default: .cache, cli, runtime
  - variable: plugins.gateway.appShell
    label: App Shell Path
    type: string
    group: Gateway
    description: |
      Absolute path to the micro-frontend shell application.
      When configured, all browser navigations are served through this shell.
  - variable: plugins.gateway.rateLimit.requests
    label: "Rate Limit: Max Requests"
    type: int
    group: Gateway
    description: Maximum requests per window
    default: 100
    min: 1
    max: 10000
  - variable: plugins.gateway.rateLimit.window
    label: "Rate Limit: Time Window"
    type: enum
    group: Gateway
    default: 1m
    options:
      - 30s
      - 1m
      - 5m
      - 1h
  - variable: plugins.gateway.cors.origin
    label: "CORS: Allowed Origins"
    type: string
    group: Gateway
    description: "* for all, or comma-separated list"
    default: "*"
  - variable: plugins.gateway.cors.credentials
    label: "CORS: Allow Credentials"
    type: boolean
    group: Gateway
    default: false
